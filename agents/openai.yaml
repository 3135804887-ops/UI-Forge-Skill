interface:
  display_name: "UI Forge"
  short_description: "Find and reconstruct local React UI components"
  default_prompt: "Use $ui-forge to find and reconstruct a ready-made React UI component from my local catalog."
policy:
  allow_implicit_invocation: true
